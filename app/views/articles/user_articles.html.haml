.row
  .large-12.columns
    #main-content.wrap
      %a.button.tiny{:href => "/articles/new"} 创建
      #u-articles
        - @articles.each do |article|
          %h4
            = link_to article.title, article_path(article.id)

    = will_paginate @articles

= content_for :js_ext do
  :javascript
    $('.my-articles-btn').addClass('active');
